[project]
name = "napari-inverted-scrolling"
version = "1.0.0"
description = "Napari plugin to invert scrolling behavior. (Scroll to move through slices, Ctrl + Scroll to zoom)"
readme = "README.md"
license = {file = "LICENSE"}
authors = [
  {name = "Tom Blöcker"},
  {email = "Tom.Bloecker@campus.lmu.de"},
]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Framework :: napari",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Image Processing",
]
requires-python = ">=3.9"
dependencies = [
    "numpy",
    "qtpy",
    "napari_toolkit"
]

[project.entry-points."napari.manifest"]
napari-inverted-scrolling = "napari_inverted_scrolling:napari.yaml"

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["*.yaml"]



